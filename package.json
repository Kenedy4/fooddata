{
  "name": "fooddata",
  "version": "1.0.0",
  "description": "",
  "main": "json-server --watch db.json --port 3001",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^1.0.0-beta.1"
  }
}
